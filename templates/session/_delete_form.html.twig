<form method="post" action="{{ path('app_session_delete', {'id': sessions.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sessions.id) }}">
    <button class="btn">Delete</button>
</form>
